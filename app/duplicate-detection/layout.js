export const metadata = {
  title: "Duplicate Detection",
  description: "Detect and remove duplicate leads from multiple CSV files. Upload your CSV files and clean your data for better lead management.",
  openGraph: {
    title: "Duplicate Detection - CULTJANI",
    description: "Detect and remove duplicate leads from multiple CSV files",
  },
};

export default function DuplicateDetectionLayout({ children }) {
  return children;
}
