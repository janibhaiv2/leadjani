export const metadata = {
  title: "Merge Leads",
  description: "Merge multiple CSV files containing leads into a single file. Upload your CSV files and combine them for easier management.",
  openGraph: {
    title: "Merge Leads - CULTJANI",
    description: "Combine multiple CSV files containing leads into a single downloadable file",
  },
};

export default function MergeLeadsLayout({ children }) {
  return children;
}
